# 두두택배 정책 반영 화면 작업 목록

## 목표

`데이터_처리_정책서_업데이트.md`의 세 가지 처리 층을 접수 화면에 반영한다.

1. **기계적 차단**: 잘못된 값은 입력 단계에서 막는다.
2. **정책 판정**: 계산 규칙을 한 곳에서 동일하게 적용한다.
3. **운영 확인**: 확정할 수 없는 값은 삭제하거나 덮어쓰지 않고 보류한다.

기존 `dudu-F-single-page/single-page.html`은 비교용으로 유지하고, 새 정책 반영판은 `dudu-F-single-page/index.html`에서 시작한다.

## 1차 구현 완료

- [x] 새 `index.html` 진입점 생성
- [x] GitHub Pages에서 동작하는 해시 route 구성
  - `#/` 대시보드
  - `#/shipments/new` 새 접수
  - `#/shipments` 접수 목록
  - `#/shipments/{trackingNo}` 접수 상세
  - `#/reviews` 보류 검토
  - `#/policy` 정책 보기
- [x] 접수 지점을 6개 표준 선택 목록으로 변경
- [x] 도착 지역을 표준 선택 목록으로 변경
- [x] 미등록 지점 코드 `00` 자동 발급 제거
- [x] 알 수 없는 지역을 일반 권역으로 자동 처리하지 않도록 변경
- [x] 부피 무게와 청구 무게 자동 계산
- [x] 크기 등급, 배송 권역, 요금, 도착 예정일 자동 계산
- [x] 대형 상한 초과 접수 차단
- [x] 금지 품목을 확정 차단과 운영 확인 대상으로 분리
- [x] 50만원 초과 시계 차단
- [x] 애매한 품목을 `운영 확인 필요` 상태로 저장
- [x] 검토 담당자와 처리 근거 기록
- [x] 입력 원본과 계산값 분리 보존
- [x] 표준 배송 상태 7종만 선택하도록 제한
- [x] 배송 상태 변경 이력 보존
- [x] 접수 목록 검색 및 상세 화면 구현
- [x] `localStorage`를 이용해 새로고침 후에도 접수 유지
- [x] 사용자 입력을 HTML에 출력할 때 이스케이프 처리
- [x] 모바일·태블릿 반응형 화면 적용

## 검산 완료

- [x] 이불 3kg, 60×40×50cm, 일반 → 대형 9,000원
- [x] 책 12kg, 30×20×20cm, 일반 → 중형 6,000원
- [x] 화장품 1.5kg, 20×15×10cm, 일반 → 극소형 3,500원
- [x] 운동화 2kg, 35×25×20cm, 일반 → 소형 4,000원
- [x] 쌀 8kg, 30×20×20cm, 일반 → 중형 6,000원
- [x] 확실한 금지 품목 자동 차단
- [x] 애매한 금지 품목 운영 확인 전환
- [x] 25kg 초과 접수 차단

## 2차 구현 완료 (Supabase 연결)

- [x] 루트에 index.html, js/, assets/ 배치
- [x] api/shipments.js로 Supabase 저장 함수 구현
- [x] submitShipment에서 API 호출 추가
- [x] Vercel 배포 (https://dudu-logistics-zero.vercel.app)
- [x] .env.example, package.json, vercel.json 생성
- [x] 환경변수 준비 (로컬 .env 생성)

## 다음 단계

### 데이터베이스 연결 (계속)

- [ ] Vercel 환경변수 설정 및 재배포
- [ ] 1건 접수 저장 후 Supabase에서 확인
- [ ] `tracking_no`에 고유 제약 추가
- [ ] 지점별 운송장 순번을 서버에서 발급
- [ ] 저장 요청에 멱등 키를 적용해 중복 클릭·재전송 차단
- [ ] 같은 운송장 번호 발견 시 기존 접수 비교 화면 제공
- [ ] 기존 접수 수정 또는 새 운송장 발급 운영 절차 확정

### 운영 정책 협의

- [ ] `반송`, `회수`, `배송실패`, `배송중`, `배달중`의 표준 상태 매핑 확정
- [ ] 조건부·애매 품목의 최종 승인 권한 확정
- [ ] 접수 가능한 개별 가로·세로·높이 최대값 확정
- [ ] 환불을 요금과 분리해 기록할 별도 구조 확정

### 품질 확인

- [ ] 실제 브라우저에서 전체 route 이동 확인
- [ ] 모바일 화면에서 입력·목록·검토 흐름 확인
- [ ] 키보드만으로 접수 가능한지 확인
- [ ] `verified_300.csv`를 사용해 계산 결과 대조
- [ ] 새로고침·중복 클릭·저장 실패 상황 확인

## 완료 기준

- 접수 1건이 저장되고 고유 운송장 번호로 다시 조회된다.
- 잘못된 값은 저장 전에 이유와 함께 차단된다.
- 확정할 수 없는 값은 원본을 유지한 채 보류 목록으로 이동한다.
- 보류 처리에는 담당자, 판단 근거, 처리 시각이 남는다.
- 계산 결과가 `02_접수규정.md`의 대표값과 일치한다.
- 다음 사람이 `README.txt`와 이 문서만 보고 실행과 후속 작업을 이어갈 수 있다.
