# 두두 x CU 택배 구현 현황

기준일: 2026-08-07

운영 진입점: 루트 `index.html`

배포 주소: https://dudu-logistics-zero.vercel.app

## 이번 구현 완료

- [x] Supabase 저장 성공 후에만 접수 완료 처리
- [x] 서버에서 `지점코드 2자리 + 무작위 숫자 8자리` 가상 운송장 발급
- [x] 요청 번호를 이용한 중복 저장 방지
- [x] 서버에서 지점·지역·금지 품목·무게·크기·등급·요금 재검증
- [x] 클라이언트가 보낸 계산값을 무시하고 서버에서 다시 계산
- [x] 접수 유형을 일반·예약·비회원·회원·쇼핑몰·점간으로 분리 저장
- [x] 관리자 대시보드와 접수 목록을 Supabase 데이터로 연결
- [x] 접수 상세 직접 접근 시 Supabase에서 다시 조회
- [x] 배송 상태 변경 API 구현
- [x] 배송 상태 변경 성공 후에만 화면과 브라우저 저장값 갱신
- [x] 관리자 메뉴의 현재 페이지 활성 표시 수정
- [x] 잘못된 예약 URL 및 잘못 인코딩된 URL 예외 처리
- [x] 예약택배 완료 화면에서 실제 저장값 표시
- [x] 완료 화면에 카카오톡 테스트 메시지 미리보기 표시
- [x] 완료 화면에 실습용 송장 PNG 생성·저장·출력 기능 추가
- [x] 송장 출력 후 부착 안내 추가
- [x] 서버 검증 자동 테스트 5건 추가
- [x] Vercel Analytics 연결

## Supabase에서 한 번 실행할 작업

`supabase-status-update.sql`을 Supabase SQL Editor에서 한 번 실행해야 관리자 배송 상태 변경이 DB에 저장된다.

기존 정책 컬럼까지 처음 설치하는 환경은 `supabase-update.sql` 전체를 실행하면 된다.

이 SQL은 기존 접수를 삭제하지 않으며 다음 항목을 적용한다.

- 접수 정책 기록용 컬럼과 제약 조건
- 운송장·요청 번호 중복 방지
- 배송 상태 변경 이력 트리거
- 실습 관리자 화면의 상태 컬럼 업데이트 권한

## 의도적으로 보류

- [ ] 접수 조회 API 인증: 실습 페이지이므로 이번 범위에서 보류
- [ ] 실제 카카오 알림톡 전송: 카카오 비즈니스 채널·발신 프로필·템플릿·알림톡 공급사 계약 필요
- [ ] 실제 택배사에서 인식하는 운송장 바코드: 현재 이미지는 화면 실습용이며 실제 운송용이 아님
- [ ] 관리자 로그인과 역할별 권한: 실사용 전 필수

## 소스 기준

- 루트 `index.html`, `js/`, `assets/`, `api/`가 현재 Vercel 배포 소스다.
- `dudu-F-single-page/single-page.html`은 최초 한 장 화면 비교본이다.
- `dudu-F-single-page/index.html`과 하위 소스는 이전 프로토타입 참고용으로 유지한다.
- 중복된 프로토타입을 수정하지 말고 새 변경은 루트 소스에만 반영한다.

## 확인 명령

```bash
npm run check
```

이 명령은 문법 검사, 자동 테스트, Analytics 번들 생성을 순서대로 실행한다.
