# 두두 로지스틱스 - 데이터 처리 정책서

- **작성자**: 이가영
- **작성일**: 2026-08-06
- **대상**: 접수 원장 `shipments` 3,045건 / 25칸

---

## 오늘 내는 것 세 가지

1. **정리 방식** - 어떤 기준으로 처리했는가
2. **오류 분류** - 어떤 예외가 나왔는가
3. **처리 방안** - 다음 단계에서 어떻게 할 것인가

셋 다 **칸 하나 안에** 들어갑니다. 이 문서는 칸 하나가 한 덩어리입니다.

정리만 하고 왜 그랬는지를 안 남기면 내일 일이 진행되지 않습니다. 검산할 때 "이걸 왜 했지"가 됩니다.

정답이 없습니다. 같은 칸을 봐도 무엇을 막을지는 사람마다 다릅니다. 조사한 칸이 세 개여도 됩니다.

---

## 조사 현황

내가 본 칸에 체크합니다. **안 본 칸도 정보입니다.**

- [x] `tracking_no` - 운송장 번호
- [ ] `accepted_at` - 접수 시각
- [ ] `branch_code` - 지점 코드
- [ ] `branch_name` - 지점 이름
- [ ] `sender_id` - 보내는 사람 ID
- [ ] `sender_name` - 보내는 사람 이름
- [ ] `receiver_name` - 받는 사람 이름
- [ ] `receiver_phone` - 받는 사람 전화
- [ ] `receiver_area` - 받는 지역
- [ ] `receiver_dong` - 받는 동
- [ ] `region_type` - 권역 구분
- [ ] `category` - 물품 분류
- [ ] `item_name` - 물품명
- [ ] `weight_kg` - 무게
- [ ] `width_cm` - 가로
- [ ] `height_cm` - 세로
- [ ] `depth_cm` - 높이
- [ ] `volume_weight_kg` - 부피 무게
- [x] `billed_weight_kg` - 청구 무게
- [ ] `size_grade` - 크기 등급
- [ ] `price` - 요금
- [ ] `eta_date` - 도착 예정일
- [x] `status` - 배송 상태
- [ ] `delivered_at` - 배송 완료 시각
- [ ] `channel` - 접수 경로

---

## 쓰기 전에 - 세 가지 눈금

칸을 채울 때 아래 셋을 골라 적습니다.

### 눈금 1. 처리 분류 - 이 예외를 무엇으로 했는가

- **수정** - 값을 고쳤다
- **삭제** - 행이나 값을 지웠다
- **통일** - 여러 표기를 하나로 묶었다
- **보류** - 손대지 않고 남겼다

### 눈금 2. 어디서 막는가 - 이 예외를 앞으로 어디서 걸러야 하는가

- **기계적 차단** - 화면이 애초에 못 넣게 한다. 필수 입력, 숫자만, 선택 목록
- **정책** - 규칙을 정해야 정해진다. 선택지를 어디까지 열지, 예외를 어디까지 허용할지
- **운영** - 사람이 받아야 한다. 상담원 연결, CS 접수, 환불 처리

세 층을 섞지 않습니다. 정책으로 정할 것을 기계로 막으면 현장이 못 씁니다. 기계로 막을 것을 사람에게 넘기면 같은 일이 계속 들어옵니다.

### 눈금 3. 확신도 - 내가 어디까지 정했는가

- **확정** - 이렇게 합니다
- **추천** - 이게 낫다고 보지만 최종 결정은 팀에서
- **선택지** - 두 갈래를 다 만들어 뒀습니다. 고르시면 됩니다

혼자 확정한 것보다 여럿이 같은 결론에 도달한 것이 믿을 만합니다. 혼자 판단하면 틀리기 쉽습니다.

---

# 칸별 정책

아래 블록을 조사한 칸 수만큼 복사해서 씁니다.

---

## 칸 - `tracking_no` (운송장 번호)

### 1. 초기 상태

- 값 종류: 3,000가지
- 빈 칸: 0건
- 형식: 숫자로만 구성된 10자리 식별번호
- 어떻게 셌나: 전체 3,045행에서 운송장 번호별 등장 횟수를 세고, 같은 번호가 두 번 이상 등장하는지 확인했다. 추가로 운송장 번호 앞 2자리와 `branch_code`가 같은지도 대조했다.

### 2. 예외

**예외 1 - 같은 운송장 번호 중복**

- 몇 건: 45개 번호, 총 90행
- 어떻게 찾았나: `tracking_no`별 등장 횟수를 세어 두 번 등장한 번호를 찾았다.

**예외 2 - 중복된 두 행의 받는 사람 이름이 다름**

- 몇 건: 중복 45쌍 중 44쌍
- 어떻게 찾았나: 같은 운송장 번호의 두 행을 원본 25개 칸 전체로 비교했다. 44쌍은 `receiver_name`만 달랐고, 1쌍은 모든 값이 같았다.

### 3. 처리

**예외 1 - 같은 운송장 번호 중복**

- 분류: 보류
- 왜 이렇게 정했나: 실제로 두 물건이 접수된 것인지, 같은 접수가 두 번 저장된 것인지 현재 접수 원장만으로 확인할 수 없다. 바로 삭제하면 실제 접수 기록 한 건이 사라질 수 있다.
- 왜 생겼다고 보는가: 저장 요청이 두 번 전송됐거나, 이미 사용한 운송장 번호가 다시 사용됐을 가능성이 있다.
- 확신도: 확정

**예외 2 - 받는 사람 이름이 다른 중복**

- 분류: 보류
- 왜 이렇게 정했나: 두 행을 완전히 같은 접수의 복사본이라고 단정할 수 없고, 어느 이름이 맞는지도 확인할 근거가 없다.
- 왜 생겼다고 보는가: 운송장 번호 재사용, 수취인 정보 수정 또는 중복 저장 과정에서 일부 값이 달라졌을 가능성이 있다.
- 확신도: 선택지

### 4. 앞으로 어디서 막나

**예외 1 - 같은 운송장 번호 중복**

- 층: 기계적 차단
- 어떻게: `tracking_no`를 중복 저장할 수 없는 고유값으로 설정한다. 저장 버튼이 여러 번 눌려도 같은 요청은 한 번만 등록되도록 한다.

**예외 2 - 이미 사용한 번호에 다른 수취인 입력**

- 층: 운영
- 어떻게: 이미 등록된 번호가 입력되면 바로 저장하지 않고 기존 접수 정보를 보여준다. 담당자가 기존 접수 수정 또는 새 운송장 발급 중 하나를 처리한다.

### 5. 못 정한 것 - 누구에게 물어야 하나

- 중복 45건이 실제 이중 등록인지 별도 물건인지 → 접수 담당자, 배송 이력 담당자
- 중복 발생 시 기존 접수를 수정할지 새 운송장을 발급할지 → 운영 정책 담당자

---

## 칸 - `status` (배송 상태)

### 1. 초기 상태

- 값 종류: 17가지
- 빈 칸: 0건
- 형식: 글자
- 어떻게 셌나: `status`의 고유값과 각 값의 등장 횟수를 세고, 데이터 사전에 있는 표준 7개 상태와 비교했다.

### 2. 예외

**예외 1 - 같은 의미로 볼 수 있는 다른 표기**

- 몇 건: 57건
- 어떻게 찾았나: 표준 상태와 비교하여 띄어쓰기, 공백 또는 다른 표현으로 입력된 값을 찾았다.
- 예시: `배송 완료`, `배송완료 `, `배달완료`, `완료`, `미 배송`, `미배달`

**예외 2 - 표준 상태와의 관계가 불명확한 값**

- 몇 건: 82건
- 어떻게 찾았나: 표준 7개 상태에 포함되지 않으며 현재 원장만으로 의미를 확정할 수 없는 값을 모았다.
- 예시: `반송`, `배송중`, `배달중`, `배송실패`, `회수`

### 3. 처리

**예외 1 - 같은 의미로 볼 수 있는 다른 표기**

- 분류: 통일
- 왜 이렇게 정했나: 표기가 다르면 같은 배송 상태가 여러 항목으로 나뉘어 배송완료율과 미배송률 집계가 달라진다. 원래 `status` 값은 유지하고 별도의 표준 상태 칸을 만들었다.
- 왜 생겼다고 보는가: 상태를 선택 목록이 아닌 자유 입력으로 받아 담당자마다 다르게 입력한 것으로 보인다.
- 확신도: 추천

**예외 2 - 의미가 불명확한 상태**

- 분류: 보류
- 왜 이렇게 정했나: `반송`을 바로 `반품`으로, `배송실패`를 바로 `미배송`으로 바꾸면 실제 업무 상태와 달라질 수 있다.
- 왜 생겼다고 보는가: 기준 상태와 현장에서 사용하는 용어가 일치하지 않았거나 자유 입력이 허용된 것으로 보인다.
- 확신도: 확정

### 4. 앞으로 어디서 막나

**예외 1 - 같은 상태의 표기 갈림**

- 층: 기계적 차단
- 어떻게: 배송 상태를 직접 입력하지 못하게 하고 표준 상태만 선택 목록으로 제공한다.

**예외 2 - 반송·회수·배송실패 등의 별도 용어**

- 층: 정책
- 어떻게: 각 용어가 표준 상태 중 어느 단계인지 정한다. 별도 상태가 실제로 필요하다면 표준 목록에 추가할지 협의한다.

### 5. 못 정한 것 - 누구에게 물어야 하나

- `반송`, `회수`, `배송실패`, `배송중`, `배달중`의 정확한 업무상 의미 → 배송 운영 담당자
- 표준 7개 상태만 사용할지 예외 상태를 추가할지 → 운영 정책 담당자

---

## 칸 - `billed_weight_kg` (청구 무게)

### 1. 초기 상태

- 값 종류: 189가지
- 빈 칸: 0건
- 형식: 숫자
- 어떻게 셌나: 기존 `billed_weight_kg`와 실제 무게 및 치수로 다시 계산한 청구 무게를 비교했다.

### 2. 예외

**예외 1 - 계산 규칙과 기존 청구 무게가 다름**

- 몇 건: 63건
- 어떻게 찾았나: 부피 무게를 `가로 × 세로 × 높이 ÷ 6000`으로 계산하고, 실제 무게와 계산 부피 무게 중 큰 값을 기존 `billed_weight_kg`와 비교했다.

**예외 2 - 원천 무게 또는 치수가 이상하여 계산을 확정할 수 없음**

- 몇 건: 비정상 치수 56건, 비정상 실제 무게 48건
- 어떻게 찾았나: 실제 무게 또는 가로·세로·높이에 0 이하 값, 숫자가 아닌 문자 또는 지나치게 큰 값이 있는 행을 찾았다.

### 3. 처리

**예외 1 - 청구 무게 불일치**

- 분류: 보류
- 왜 이렇게 정했나: 계산값과 기존값은 다르지만 실제 무게나 치수 중 어느 값이 잘못됐는지 현재 원장만으로 확정할 수 없다. 기존값은 유지하고 계산 후보값을 새 칸에 만들었다.
- 왜 생겼다고 보는가: 청구 무게를 사람이 직접 입력했거나 부피 무게가 수정된 뒤 청구 무게가 다시 계산되지 않았을 가능성이 있다.
- 확신도: 추천

**예외 2 - 무게 또는 치수 이상**

- 분류: 보류
- 왜 이렇게 정했나: 잘못된 무게나 치수를 사용하면 계산 결과도 잘못된다. 원천값을 임의로 고치지 않고 계산 대상에서 제외했다.
- 왜 생겼다고 보는가: 단위 문자 혼입, 입력 실수, 소수점 누락 또는 임시값 입력 가능성이 있다.
- 확신도: 확정

### 4. 앞으로 어디서 막나

**예외 1 - 청구 무게 수동 입력**

- 층: 기계적 차단
- 어떻게: 청구 무게 입력칸을 없애고 실제 무게와 치수가 입력되면 시스템이 자동 계산하도록 한다.

**예외 2 - 무게·치수 이상값**

- 층: 기계적 차단
- 어떻게: 숫자만 입력할 수 있게 하고 0 이하 값은 저장하지 못하게 한다. 최대 무게와 치수를 넘으면 바로 저장하지 않고 확인 안내를 표시한다.

### 5. 못 정한 것 - 누구에게 물어야 하나

- 실제 접수 가능한 최대 무게와 최대 치수 → 택배 운영 담당자
- 계산 청구 무게와 기존값이 다를 때 자동 적용할지 사람 승인을 받을지 → 요금 정책·정산 담당자
- 불일치 63건에서 기존값과 계산값 중 무엇이 실제 접수값인지 → 원접수 기록 담당자

---

# 채운 예시 두 개

베껴도 되고 전혀 다르게 써도 됩니다.

---

## 칸 - `price` (요금)

### 1. 초기 상태

- 값 종류: 17가지 (빈 칸 포함)
- 빈 칸: 215건
- 형식: 숫자
- 어떻게 셌나: 필터 메뉴에 `17개 표시 중`. 빈 칸만 골라 화면 오른쪽 아래의 `총 3,045행 중 215행 표시됨`

### 2. 예외

**예외 1 - 빈 칸**

- 몇 건: 215
- 어떻게 찾았나: 필터 > 조건별 필터링 > `(공백)`

**예외 2 - 음수 요금 (`-1000`, `-1500`)**

- 몇 건: 4
- 어떻게 찾았나: 필터 값 목록 맨 위

### 3. 처리

**예외 1 - 빈 칸 215건**

- 분류: 보류
- 왜 이렇게 정했나: 안 받은 돈인지 안 적은 것인지 모른다. 0으로 채우면 못 받은 돈이 받은 것이 된다
- 왜 생겼다고 보는가: 접수 담당자가 나중에 적으려다 잊었을 가능성
- 확신도: 확정

**예외 2 - 음수 4건**

- 분류: 보류
- 왜 이렇게 정했나: 요금이 마이너스일 수는 없지만 지우면 그 건의 흔적이 사라진다
- 왜 생겼다고 보는가: 환불을 요금 칸에 마이너스로 적은 것으로 보임
- 확신도: 추천

### 4. 앞으로 어디서 막나

**예외 1 - 빈 칸**

- 층: 기계적 차단
- 어떻게: 요금은 사람이 입력하지 않는다. 무게와 지역으로 계산해서 자동으로 채운다. 계산이 안 되면 저장을 막는다

**예외 2 - 음수**

- 층: 정책
- 어떻게: 환불을 요금 칸에 적을 것인지 별도 칸을 둘 것인지 정해야 한다

### 5. 못 정한 것 - 누구에게

- 빈 215건이 미수금인지 기록 누락인지 -> 접수 담당자
- 환불 표기 방식 -> 정산 담당

---

## 칸 - `receiver_area` (받는 지역)

### 1. 초기 상태

- 값 종류: 28가지
- 빈 칸: 0건
- 형식: 글자, 자유 입력으로 보임
- 어떻게 셌나: 필터 메뉴 `28개 표시 중`

### 2. 예외

**예외 1 - 같은 지역을 다르게 적음 (`서울` / `서울시` / `서울특별시`)**

- 몇 건: 세어 보지 못함
- 어떻게 찾았나: 필터 값 목록이 가나다순이라 붙어 있음

**예외 2 - 영문 표기 (`SEOUL`)**

- 몇 건: 세어 보지 못함
- 어떻게 찾았나: 목록 끝쪽

### 3. 처리

**예외 1 - 같은 지역 다른 표기**

- 분류: 통일
- 왜 이렇게 정했나: 집계가 안 맞는다. 서울이 세 덩어리로 갈려 있으면 지점별 물량을 못 센다
- 왜 생겼다고 보는가: 자유 입력이라 사람마다 다르게 적었다. 담당자가 바뀌면서 표기도 바뀐 것으로 보임
- 확신도: 확정

**예외 2 - 표준 목록을 무엇으로 할지**

- 분류: 보류
- 왜 이렇게 정했나: 광역시도 17개로 할지 배송 권역으로 할지에 따라 요금이 달라진다
- 왜 생겼다고 보는가: 애초에 정해진 목록이 없었다
- 확신도: 선택지 (둘 다 만들어 뒀습니다)

### 4. 앞으로 어디서 막나

**예외 1 - 표기 갈림**

- 층: 기계적 차단
- 어떻게: 자유 입력을 없애고 선택 목록으로 받는다. 손으로 치면 또 갈린다

**예외 2 - 목록의 범위**

- 층: 정책
- 어떻게: 어느 단위로 나눌지는 요금 규정과 붙어 있어 우리가 못 정한다

### 5. 못 정한 것 - 누구에게

- 표준 목록 단위 -> 현장

---

# 마무리

## 검산

정한 대로 실제로 됐는지 대 봅니다. 맞히려는 것이 아니라 **어디가 다른지 보려는 것**입니다.

**칸 - `tracking_no`**

- 처리 전: 전체 3,045행, 고유 운송장 번호 3,000개, 중복 번호 45개
- 처리 후: 원본 3,045행은 모두 유지하고 중복된 90행에 중복 여부와 보류 사유를 추가했다
- 예상과 달랐던 것: 중복 45쌍이 모두 같은 행일 것으로 생각했지만, 44쌍은 `receiver_name` 값이 달랐고 1쌍만 완전히 같았다

**칸 - `status`**

- 처리 전: 배송 상태가 17가지 표현으로 나뉘어 있었다
- 처리 후: 의미가 확실한 57건은 표준 상태 후보를 만들고, 의미가 불명확한 82건은 보류했다
- 예상과 달랐던 것: 모든 비표준 상태를 표준 7개 상태에 바로 넣을 수 없었다

**칸 - `billed_weight_kg`**

- 처리 전: 원장에 입력된 청구 무게를 그대로 사용하고 있었다
- 처리 후: 실제 무게와 계산 부피 무게 중 큰 값을 계산 후보로 만들고 기존값과 다른 63건을 확인 대상으로 표시했다
- 예상과 달랐던 것: 청구 무게만 비교하면 끝나는 것이 아니라 실제 무게와 가로·세로·높이의 이상 여부도 함께 확인해야 했다

## 동료와 대 보기

**칸 - `tracking_no`**

- 나의 판단: 중복 번호는 바로 삭제하지 않고 실제 이중 등록 여부를 확인할 때까지 보류
- 다른 사람 판단: 완전 중복으로 보고 45행을 삭제하여 3,000행으로 정리
- 같았나: 다름. 실제 비교에서 44쌍의 받는 사람 이름이 달랐으므로 내 작업에서는 보류를 유지했다

**칸 - `status`**

- 나의 판단: 명확한 표기 차이만 통일하고 의미가 불명확한 상태는 보류
- 다른 사람 판단: 비표준 상태를 표준 7개 상태로 모두 매핑
- 같았나: 일부만 같음. `반송`, `회수`, `배송실패` 등은 업무 정의를 확인하기 전까지 보류했다

**칸 - `billed_weight_kg`**

- 나의 판단: 원천 무게와 치수가 정상인 행에서만 계산값을 비교하고 63건을 불일치로 확인
- 다른 사람 판단: 정제 기준에 따라 161건 또는 179건을 수정 대상으로 확인
- 같았나: 다름. 단위 제거, 이상값 처리, 반올림 기준이 달라질 수 있어 계산 기준을 먼저 맞춰야 한다

## 원본 보존

- [x] 원래 칸은 그대로 두고 새 칸을 옆에 만들었다
- [ ] 원래 칸을 덮어썼다

새로 만든 칸:

- `tracking_no` 관련: 운송장 지점 코드, 중복 여부, 운송장 판정
- `status` 관련: 표준 배송 상태, 배송 상태 판정
- `billed_weight_kg` 관련: 계산 부피 무게, 계산 청구 무게, 무게 판정
- 공통 관리: 보류 사유, 처리 방안, 필요한 추가 자료, 재검토 완료 조건

원래 칸을 덮어썼다면 되돌릴 방법:

- 원래 칸은 덮어쓰지 않았으므로 별도 복구가 필요하지 않다

## 이번에 다루지 않기로 한 것

- 공식 요금표가 없으므로 `price`의 정확한 정상 요금과 추가 청구액은 확정하지 않았다
- 중복 운송장 행은 배송 추적 기록과 원접수 기록이 없으므로 삭제하지 않았다
- 전화번호 결측값과 앞자리 누락 의심값은 임의로 복원하지 않았다
- `반송`, `회수`, `배송실패` 등 업무 의미가 불명확한 상태는 임의로 매핑하지 않았다

## 다음 사람에게

- 원본 25개 칸과 3,045행은 수정하거나 삭제하지 않았다
- 표준값과 계산값은 모두 `[추가]` 칸에 있으며 분석할 때 원본 대신 해당 표준값을 사용한다
- `보류`는 오류가 없다는 뜻이 아니라, 현재 자료만으로 정답을 확정할 수 없다는 뜻이다
- 중복 행을 삭제하거나 청구 무게를 확정하려면 `tracking_events.csv`, 원접수 기록, 공식 접수 규정이 필요하다

## 막힌 자리

- 중복 운송장이 실제 이중 저장인지 별도 물건인지 확인할 자료가 없음
- 비표준 배송 상태의 업무상 의미를 확인할 담당자 또는 상태 정책이 없음
- 접수 가능한 최대 무게·치수와 공식 요금 기준이 없어 청구 무게 이후의 요금까지 확정하지 못함

---

## 내일 미리 보기

내일은 접수 키오스크 입력 폼을 만듭니다. 택배 정책은 CJ대한통운, 화면은 CU 편의점 키오스크를 참고합니다.

각 칸의 **4번(어디서 막나)** 이 그대로 내일의 설계도입니다.

- **기계적 차단**으로 표시한 것 -> 내일 화면에서 구현한다
- **정책**으로 표시한 것 -> 선택지를 열어 두고 협의 대상으로 남긴다
- **운영**으로 표시한 것 -> 상담원 연결이나 문의 버튼으로 뺀다

키오스크를 안 써봤으면 유튜브 브이로그를 5개 봅니다. 화면이 바뀌는 단위로 따라가며 보면 대부분 해결됩니다. 5개 중 3개 이상에서 같은 것이 반복되면 그건 그렇다고 봐도 됩니다.
